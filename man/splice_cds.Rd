% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_cds.R
\name{splice_cds}
\alias{splice_cds}
\title{Splice CDS}
\usage{
splice_cds(dt, genome_path, ID = NULL, seqnames_field = NULL, splice = TRUE)
}
\arguments{
\item{dt}{A \code{data.table} (or coercible object) containing genomic features
with at minimum columns for seqname, start, end, strand, and attributes.}

\item{genome_path}{Character. Path to an indexed FASTA file.}

\item{ID}{Character or \code{NULL}. Name of the column in \code{dt} to use as
the feature ID for grouping. If \code{NULL}, the ID is parsed from the
\code{attributes} column.}

\item{seqnames_field}{Character or \code{NULL}. Name of the column in \code{dt}
containing sequence names. If \code{NULL}, defaults to the column matching
\code{"seqid"}.}

\item{splice}{Logical. If \code{TRUE} (default), returns spliced sequences as a
\code{DNAStringSet} with one entry per feature ID. If \code{FALSE}, returns
unspliced per-range sequences.}
}
\value{
A \code{DNAStringSet} of spliced (or unspliced) sequences, named by
  feature ID.
}
\description{
Extracts and splices CDS (or other) sequences from a genome FASTA file given
a data.table of genomic features. Handles strand-aware sorting so minus-strand
features are concatenated in the correct order.
}
